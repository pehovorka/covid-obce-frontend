import makeStyles from '@mui/styles/makeStyles';

export const useStyles = makeStyles(() => ({
  title: {
    textAlign: "center",
    fontSize: "0.9rem",
  },
  text: {
    textAlign: "center",
  },
  bold: {
    fontWeight: 500,
  },
}));
