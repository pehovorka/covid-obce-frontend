import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() => ({
  title: {
    textAlign: "center",
    fontSize: "0.9rem",
  },
  text: {
    textAlign: "center",
  },
  bold: {
    fontWeight: 500,
  },
}));
